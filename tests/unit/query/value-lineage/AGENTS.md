<!-- generated by espalier — do not edit — source: espalier/value-lineage -->

# pgsid query analysis tests — `value-lineage/`

Each fixture isolates one transformation or composition rule. Keep the schema
small and stable so fixture changes describe lineage behavior rather than
unrelated catalog differences.

The `dml/` tier composes transformations across write assignments, row images,
and data-modifying CTEs. Its fixtures may deliberately combine several syntax
features when their interaction is the contract under test.

## Project layout

    value-lineage/
    ├─ dml/           hand-authored DML value-lineage shape contracts
    │
    ├─ schema.sql     the shared schema for value-lineage fixtures (required)
    │
    └─ [fixture].sql  a focused value-lineage query contract (zero or more, any
                      name)

## `schema.sql` — the shared schema for value-lineage fixtures

Define only the database objects needed to resolve the value-lineage fixture expressions.

## `[fixture].sql` — a focused value-lineage query contract

Keep one representative transformation shape per fixture and give every projected value a stable alias.

## Other repository paths

These paths are part of the repository but serve specific purposes outside the project layout. Use them only for the purpose stated here.

| Paths | Purpose |
| --- | --- |
| `AGENTS.md` | Generated repository guidance; edits here will be overwritten. Modify [`ESPALIER.MD`](../espalier/value-lineage/ESPALIER.MD) to change this document's persistent guidance. |

Every path under value-lineage/ must fit the project layout or a documented
repository-specific purpose.
