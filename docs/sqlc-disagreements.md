# sqlc disagreement register

Generated by `tests/probe/sqlc-register.ts` from the vendored corpus
(`tests/unit/query/sqlc-corpus/`, upstream release pinned in its
PROVENANCE.md). The census behind it is pinned in `sqlc-corpus.test.ts`;
regenerate after any corpus refresh or engine change that moves those pins.

## Adjudication protocol

Neither engine is presumed right; only a counterexample with data decides.

- **sqlc-stronger** (sqlc claims NOT NULL, the walk claims nullable): build
  a data state under the case's schema and run the query. A NULL in the
  column CONVICTS sqlc — the state + query is the repro for an upstream
  ticket. No witness after honest attempts → read the walk's trace
  (`inferNullabilityTraced`) for the rule that should have proven it: a
  pgsid imprecision candidate — fixture it.
- **pgsid-stronger** (the walk claims notNull, sqlc claims nullable): sqlc
  conservatism is expected in bulk (it has no promotion, key, or CHECK
  reasoning) and is not ticket-worthy. A witness NULL here would be a PGSID
  UNSOUNDNESS — top severity, before anything else.
- Record every outcome in the Disposition line: `ticket <url>`,
  `pgsid-fixed <commit>`, `conservatism-expected <why>`, or
  `unresolved <what blocks>`.

Entries: 40 (sqlc-stronger 25, pgsid-stronger 14, shape-skew 1).

## sqlc-stronger — `accurate_cte/GetProductStats` column 1 (`avg_price`)

sqlc: NOT NULL · walk: nullable · schema: `tests/unit/query/sqlc-corpus/cases/accurate_cte/schema.sql`

```sql
WITH product_stats AS (
    SELECT COUNT(*) as total, AVG(price) as avg_price FROM products
)
SELECT * FROM product_stats;
```

**Disposition:** _unresolved_

## sqlc-stronger — `builtins/Scale` column 0 (`scale`)

sqlc: NOT NULL · walk: nullable · schema: `tests/unit/query/sqlc-corpus/cases/builtins/schema.sql`

```sql
SELECT scale(8.41);
```

**Disposition:** _unresolved_

## pgsid-stronger — `coalesce_as/SumBaz` column 1 (`quantity`)

sqlc: nullable · walk: notNull · schema: `tests/unit/query/sqlc-corpus/cases/coalesce_as/schema.sql`

```sql
SELECT bar, coalesce(sum(baz), 0) as quantity
FROM foo
GROUP BY 1;
```

**Disposition:** _unresolved_

## sqlc-stronger — `create_table_as/GetFirst` column 0 (`val`)

sqlc: NOT NULL · walk: nullable · schema: `tests/unit/query/sqlc-corpus/cases/create_table_as/schema.sql`

```sql
SELECT * FROM second_table;
```

**Disposition:** _unresolved_

## pgsid-stronger — `create_view/GetSecond` column 1 (`val2`)

sqlc: nullable · walk: notNull · schema: `tests/unit/query/sqlc-corpus/cases/create_view/schema.sql`

```sql
SELECT * FROM second_view WHERE val2 = $1;
```

**Disposition:** _unresolved_

## pgsid-stronger — `cte_recursive_star/GetDictTree` column 5 (`path`)

sqlc: nullable · walk: notNull · schema: `tests/unit/query/sqlc-corpus/cases/cte_recursive_star/schema.sql`

```sql
with recursive dictTree(id, code, parent_code, label, value, path, depth) AS (
	select id, code, parent_code, label, value, ARRAY[COALESCE((select id from dict where code=''),'virtual_root'), id], 1 as depth from dict where app_id = '1' and parent_code = '' and is_delete=false
	union
		select d.id, d.code, d.parent_code, d.label, d.value, t.path || ARRAY[d.id], t.depth+1 as depth from dict d join dictTree t on d.parent_code = t.code and not d.id = ANY(t.path) and d.is_delete=false
)
select * from dictTree d order by depth, parent_code;
```

**Disposition:** _unresolved_

## sqlc-stronger — `ddl_create_table_inherits/GetAllOrganisations` column 2 (`legal_name`)

sqlc: NOT NULL · walk: nullable · schema: `tests/unit/query/sqlc-corpus/cases/ddl_create_table_inherits/schema.sql`

```sql
SELECT * FROM organisation;
```

**Disposition:** _unresolved_

## pgsid-stronger — `emit_result_and_params_struct_pointers/GetOne` column 0 (`a`)

sqlc: nullable · walk: notNull · schema: `tests/unit/query/sqlc-corpus/cases/emit_result_and_params_struct_pointers/schema.sql`

```sql
SELECT * FROM foo WHERE a = $1 AND b = $2 LIMIT 1;
```

**Disposition:** _unresolved_

## pgsid-stronger — `emit_result_and_params_struct_pointers/GetOne` column 1 (`b`)

sqlc: nullable · walk: notNull · schema: `tests/unit/query/sqlc-corpus/cases/emit_result_and_params_struct_pointers/schema.sql`

```sql
SELECT * FROM foo WHERE a = $1 AND b = $2 LIMIT 1;
```

**Disposition:** _unresolved_

## sqlc-stronger — `func_aggregate/Percentile` column 0 (`percentile_disc`)

sqlc: NOT NULL · walk: nullable · schema: `tests/unit/query/sqlc-corpus/cases/func_aggregate/schema.sql`

```sql
select percentile_disc(0.5) within group (order by authors.name)
from authors;
```

**Disposition:** _unresolved_

## sqlc-stronger — `func_call_cast/Demo` column 0 (`col1`)

sqlc: NOT NULL · walk: nullable · schema: `tests/unit/query/sqlc-corpus/cases/func_call_cast/schema.sql`

```sql
SELECT uuid_generate_v5('7c4597a0-8cfa-4c19-8da0-b8474a36440d', $1)::uuid as col1;
```

**Disposition:** _unresolved_

## shape-skew — `func_star_expansion/TestFuncSelectBlog`

sqlc IR arity 1, walk (and PostgreSQL) 4.

```sql
select * from test_func_select_blog($1);
```

**Disposition:** _unresolved_

## pgsid-stronger — `join_full/FullJoin` column 0 (`id`)

sqlc: nullable · walk: notNull · schema: `tests/unit/query/sqlc-corpus/cases/join_full/schema.sql`

```sql
SELECT f.id, f.bar_id, b.id
FROM foo f
FULL OUTER JOIN bar b ON b.id = f.bar_id
WHERE f.id = $1;
```

**Disposition:** _unresolved_

## pgsid-stronger — `join_inner/SelectAllJoinedAlias` column 0 (`id`)

sqlc: nullable · walk: notNull · schema: `tests/unit/query/sqlc-corpus/cases/join_inner/schema.sql`

```sql
select e.* from events e
    inner join handled_events he
       on e.ID > he.last_handled_id
where he.handler = $1
    for update of he skip locked;
```

**Disposition:** _unresolved_

## pgsid-stronger — `join_inner/SelectAllJoined` column 0 (`id`)

sqlc: nullable · walk: notNull · schema: `tests/unit/query/sqlc-corpus/cases/join_inner/schema.sql`

```sql
select events.* from events
    inner join handled_events
       on events.ID > handled_events.last_handled_id
where handled_events.handler = $1
    for update of handled_events skip locked;
```

**Disposition:** _unresolved_

## pgsid-stronger — `join_right/RightJoin` column 0 (`id`)

sqlc: nullable · walk: notNull · schema: `tests/unit/query/sqlc-corpus/cases/join_right/schema.sql`

```sql
SELECT f.id, f.bar_id, b.id
FROM foo f
RIGHT JOIN bar b ON b.id = f.bar_id
WHERE f.id = $1;
```

**Disposition:** _unresolved_

## pgsid-stronger — `join_right/RightJoin` column 1 (`bar_id`)

sqlc: nullable · walk: notNull · schema: `tests/unit/query/sqlc-corpus/cases/join_right/schema.sql`

```sql
SELECT f.id, f.bar_id, b.id
FROM foo f
RIGHT JOIN bar b ON b.id = f.bar_id
WHERE f.id = $1;
```

**Disposition:** _unresolved_

## sqlc-stronger — `min_max_date/ActivityStats` column 1 (`mindate`)

sqlc: NOT NULL · walk: nullable · schema: `tests/unit/query/sqlc-corpus/cases/min_max_date/schema.sql`

```sql
SELECT COUNT(*) as NumOfActivities,
        MIN(event_time) as MinDate, 
        MAX(event_time) as MaxDate 
FROM activities 
WHERE account_id = $1;
```

**Disposition:** _unresolved_

## sqlc-stronger — `min_max_date/ActivityStats` column 2 (`maxdate`)

sqlc: NOT NULL · walk: nullable · schema: `tests/unit/query/sqlc-corpus/cases/min_max_date/schema.sql`

```sql
SELECT COUNT(*) as NumOfActivities,
        MIN(event_time) as MinDate, 
        MAX(event_time) as MaxDate 
FROM activities 
WHERE account_id = $1;
```

**Disposition:** _unresolved_

## sqlc-stronger — `nextval/GetNextID` column 0 (`pk`)

sqlc: NOT NULL · walk: nullable · schema: `tests/unit/query/sqlc-corpus/cases/nextval/schema.sql`

```sql
SELECT pk, pk FROM
 (SELECT nextval('authors_id_seq') as pk) AS alias;
```

**Disposition:** _unresolved_

## sqlc-stronger — `nextval/GetNextID` column 1 (`pk`)

sqlc: NOT NULL · walk: nullable · schema: `tests/unit/query/sqlc-corpus/cases/nextval/schema.sql`

```sql
SELECT pk, pk FROM
 (SELECT nextval('authors_id_seq') as pk) AS alias;
```

**Disposition:** _unresolved_

## sqlc-stronger — `null_if_type/GetRestrictedId` column 0 (`restricted_id`)

sqlc: NOT NULL · walk: nullable · schema: `tests/unit/query/sqlc-corpus/cases/null_if_type/schema.sql`

```sql
SELECT
  NULLIF(id, $1) restricted_id
FROM
  author;
```

**Disposition:** _unresolved_

## pgsid-stronger — `omit_unused_structs/query_param_enum_table` column 2 (`value`)

sqlc: nullable · walk: notNull · schema: `tests/unit/query/sqlc-corpus/cases/omit_unused_structs/schema.sql`

```sql
SELECT * FROM query_param_enum_table WHERE value = $1;
```

**Disposition:** _unresolved_

## pgsid-stronger — `params_two/FooByAandB` column 0 (`a`)

sqlc: nullable · walk: notNull · schema: `tests/unit/query/sqlc-corpus/cases/params_two/schema.sql`

```sql
SELECT a, b FROM foo 
WHERE a = $1 and b = $2;
```

**Disposition:** _unresolved_

## pgsid-stronger — `params_two/FooByAandB` column 1 (`b`)

sqlc: nullable · walk: notNull · schema: `tests/unit/query/sqlc-corpus/cases/params_two/schema.sql`

```sql
SELECT a, b FROM foo 
WHERE a = $1 and b = $2;
```

**Disposition:** _unresolved_

## sqlc-stronger — `pg_advisory_xact_lock/AdvisoryLockOne` column 0 (`pg_advisory_lock`)

sqlc: NOT NULL · walk: nullable · schema: `tests/unit/query/sqlc-corpus/cases/pg_advisory_xact_lock/schema.sql`

```sql
SELECT pg_advisory_lock($1);
```

**Disposition:** _unresolved_

## sqlc-stronger — `pg_advisory_xact_lock/AdvisoryUnlock` column 0 (`pg_advisory_unlock`)

sqlc: NOT NULL · walk: nullable · schema: `tests/unit/query/sqlc-corpus/cases/pg_advisory_xact_lock/schema.sql`

```sql
SELECT pg_advisory_unlock($1);
```

**Disposition:** _unresolved_

## sqlc-stronger — `pg_generate_series/GenerateSeries` column 0 (`generate_series`)

sqlc: NOT NULL · walk: nullable · schema: `tests/unit/query/sqlc-corpus/cases/pg_generate_series/schema.sql`

```sql
SELECT generate_series($1::timestamp, $2::timestamp, '10 hours');
```

**Disposition:** _unresolved_

## pgsid-stronger — `returning/DeleteUserAndReturnUser` column 0 (`name`)

sqlc: nullable · walk: notNull · schema: `tests/unit/query/sqlc-corpus/cases/returning/schema.sql`

```sql
DELETE FROM users
  WHERE name = $1
  RETURNING *;
```

**Disposition:** _unresolved_

## sqlc-stronger — `sql_syntax_calling_funcs/PositionalNotation` column 0 (`concat_lower_or_upper`)

sqlc: NOT NULL · walk: nullable · schema: `tests/unit/query/sqlc-corpus/cases/sql_syntax_calling_funcs/schema.sql`

```sql
SELECT concat_lower_or_upper('Hello', 'World', true);
```

**Disposition:** _unresolved_

## sqlc-stronger — `sql_syntax_calling_funcs/PositionalNoDefaault` column 0 (`concat_lower_or_upper`)

sqlc: NOT NULL · walk: nullable · schema: `tests/unit/query/sqlc-corpus/cases/sql_syntax_calling_funcs/schema.sql`

```sql
SELECT concat_lower_or_upper('Hello', 'World');
```

**Disposition:** _unresolved_

## sqlc-stronger — `sql_syntax_calling_funcs/NamedNotation` column 0 (`concat_lower_or_upper`)

sqlc: NOT NULL · walk: nullable · schema: `tests/unit/query/sqlc-corpus/cases/sql_syntax_calling_funcs/schema.sql`

```sql
SELECT concat_lower_or_upper(a => 'Hello', b => 'World');
```

**Disposition:** _unresolved_

## sqlc-stronger — `sql_syntax_calling_funcs/NamedAnyOrder` column 0 (`concat_lower_or_upper`)

sqlc: NOT NULL · walk: nullable · schema: `tests/unit/query/sqlc-corpus/cases/sql_syntax_calling_funcs/schema.sql`

```sql
SELECT concat_lower_or_upper(a => 'Hello', b => 'World', uppercase => true);
```

**Disposition:** _unresolved_

## sqlc-stronger — `sql_syntax_calling_funcs/NamedOtherOrder` column 0 (`concat_lower_or_upper`)

sqlc: NOT NULL · walk: nullable · schema: `tests/unit/query/sqlc-corpus/cases/sql_syntax_calling_funcs/schema.sql`

```sql
SELECT concat_lower_or_upper(a => 'Hello', uppercase => true, b => 'World');
```

**Disposition:** _unresolved_

## sqlc-stronger — `sql_syntax_calling_funcs/MixedNotation` column 0 (`concat_lower_or_upper`)

sqlc: NOT NULL · walk: nullable · schema: `tests/unit/query/sqlc-corpus/cases/sql_syntax_calling_funcs/schema.sql`

```sql
SELECT concat_lower_or_upper('Hello', 'World', uppercase => true);
```

**Disposition:** _unresolved_

## sqlc-stronger — `star_expansion_series/CountAlertReportBy` column 0 (`datetime`)

sqlc: NOT NULL · walk: nullable · schema: `tests/unit/query/sqlc-corpus/cases/star_expansion_series/schema.sql`

```sql
select DATE_TRUNC($1,ts)::text as datetime,coalesce(count,0) as count from 
(
    SELECT  DATE_TRUNC($1,eventdate) as hr ,count(*)
    FROM    alertreport
    where eventdate between $2 and $3
    GROUP BY   1
) AS cnt 
right outer join ( SELECT * FROM generate_series ( $2, $3, CONCAT('1 ',$1)::interval) AS ts ) as dte
on DATE_TRUNC($1, ts ) = cnt.hr 
order by 1 asc;
```

**Disposition:** _unresolved_

## sqlc-stronger — `subquery_calculated_column/SubqueryCalcColumn` column 0 (`sum`)

sqlc: NOT NULL · walk: nullable · schema: `tests/unit/query/sqlc-corpus/cases/subquery_calculated_column/schema.sql`

```sql
SELECT sum FROM (SELECT a + b AS sum FROM foo) AS f;
```

**Disposition:** _unresolved_

## sqlc-stronger — `sum_type/SumOrder` column 0 (`sum`)

sqlc: NOT NULL · walk: nullable · schema: `tests/unit/query/sqlc-corpus/cases/sum_type/schema.sql`

```sql
SELECT SUM(quantity) FROM orders;
```

**Disposition:** _unresolved_

## sqlc-stronger — `unnest_with_ordinality/GetValues` column 2 (`value`)

sqlc: NOT NULL · walk: nullable · schema: `tests/unit/query/sqlc-corpus/cases/unnest_with_ordinality/schema.sql`

```sql
SELECT id, index::bigint, value::text
FROM array_values AS x, unnest(values) WITH ORDINALITY AS y (value, index);
```

**Disposition:** _unresolved_

## sqlc-stronger — `valid_group_by_reference/ListMetrics` column 2 (`avg`)

sqlc: NOT NULL · walk: nullable · schema: `tests/unit/query/sqlc-corpus/cases/valid_group_by_reference/schema.sql`

```sql
SELECT date_trunc('day', time) AS bucket, city_name, AVG(temp_c)
FROM weather_metrics
WHERE time > NOW() - (6 * INTERVAL '1 month')
GROUP BY bucket, city_name
ORDER BY bucket DESC;
```

**Disposition:** _unresolved_
